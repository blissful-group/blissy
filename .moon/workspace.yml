# https://moonrepo.dev/docs/config/workspace
$schema: "./cache/schemas/workspace.json"

projects:
  globs:
    - "packages/core/*"
    - "!packages/core"
    - "packages/*"
    - "!packages/"
    - "tooling/*"
  sources:
    blissy: .

vcs:
  defaultBranch: "main"
