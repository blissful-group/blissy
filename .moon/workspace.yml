# https://moonrepo.dev/docs/config/workspace
$schema: "./cache/schemas/workspace.json"

projects:
  globs:
    - "packages/core/*"
    - "packages/*"
    - "services/*"
    - "tooling/*"
  sources:
    blissy: .

vcs:
  defaultBranch: "main"
