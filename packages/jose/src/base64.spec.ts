import { it } from "vitest";

import { Base64 } from "./base64";

it.todo("encodes bytes using URL-safe base64 without padding");
it.todo("decodes URL-safe base64 without padding");
it.todo("rejects base64url input containing whitespace");
it.todo("rejects base64url input containing non-url-safe characters");
it.todo("rejects malformed base64url input");
