# AGENTS

## Workflow

- This repo uses Moon.
- Run tasks with `moon run <package>:<command>`.
- Use the Moon documentation when needed: https://moonrepo.dev/docs.

## Changeset

- This repo uses changesets cli
- Create a changeset entry to cover affected projects
